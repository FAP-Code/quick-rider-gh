// Future migrations are added here as new db.version() calls
// Version 1 is defined in db/index.ts
// Example:
// db.version(2).stores({ newTable: 'id, field1' }).upgrade(async tx => { ... })
export {}
